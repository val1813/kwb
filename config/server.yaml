server:
  host: 0.0.0.0
  port: 8080
  jwt_secret: ${JWT_SECRET}
  metadata_db: ./data/metadata.db

llm:
  # 默认使用本地Ollama，也支持任意OpenAI兼容接口
  base_url: ${LLM_BASE_URL}
  api_key: ${LLM_API_KEY}
  model: ${LLM_MODEL}
  temperature: 0.1
  max_tokens: 2048
